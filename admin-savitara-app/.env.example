# ============================================================
# Admin Savitara App (Expo React Native) — Environment Variables
# ============================================================
# 1. Copy:  cp .env.example .env
# 2. Expo reads EXPO_PUBLIC_ vars into app.config.js extra{}
# 3. For physical device on LAN, replace localhost with your machine IP
# ============================================================

# ── API ───────────────────────────────────────────────────────
# Dev (simulator):
EXPO_PUBLIC_API_BASE_URL=http://localhost:8000/api/v1
# Dev (physical device on LAN — replace XXX with your machine's IP):
# EXPO_PUBLIC_API_BASE_URL=http://192.168.1.XXX:8000/api/v1
# Production:
# EXPO_PUBLIC_API_BASE_URL=https://api.savitara.com/api/v1

# ── Google OAuth ──────────────────────────────────────────────
# Separate client ID for each platform in Firebase/Google Console
EXPO_PUBLIC_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# ── Firebase (Push Notifications via FCM) ─────────────────────
# Get these from Firebase Console → Project Settings → Your apps
# Android: place google-services.json in android/app/
# iOS:     place GoogleService-Info.plist in ios/AdminSavitaraApp/
#          (or configure via Expo config plugin in managed workflow —
#           see https://docs.expo.dev/push-notifications/fcm-credentials/)
EXPO_PUBLIC_FIREBASE_API_KEY=your-firebase-api-key
EXPO_PUBLIC_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
EXPO_PUBLIC_FIREBASE_PROJECT_ID=your-project-id
EXPO_PUBLIC_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
EXPO_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=123456789
EXPO_PUBLIC_FIREBASE_APP_ID=1:123456789:app:abcdef

# ── Razorpay ──────────────────────────────────────────────────
# Use rzp_test_ key in dev, rzp_live_ key in production
EXPO_PUBLIC_RAZORPAY_KEY=rzp_test_your_key_here

# ── Sentry (optional) ────────────────────────────────────────
# EXPO_PUBLIC_SENTRY_DSN=https://xxx@oXXX.ingest.sentry.io/XXX

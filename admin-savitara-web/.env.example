# ============================================================
# Admin Savitara Web (Next.js) — Environment Variables
# ============================================================
# 1. Copy:  cp .env.example .env.local  (Next.js reads .env.local first)
# 2. Only NEXT_PUBLIC_ vars are exposed to the browser.
# 3. Server-only vars (no prefix) are safe for secrets.
# ============================================================

# ── API ───────────────────────────────────────────────────────
# Dev:        http://localhost:8000/api/v1
# Production: https://api.savitara.com/api/v1
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000/api/v1

# ── App Meta ──────────────────────────────────────────────────
NEXT_PUBLIC_APP_NAME=Savitara Admin
NEXT_PUBLIC_APP_VERSION=1.0.0
NEXT_PUBLIC_ENV=development              # development | staging | production

# ── Sentry (optional — server-side only, no NEXT_PUBLIC prefix) ──
# SENTRY_DSN=https://xxx@oXXX.ingest.sentry.io/XXX
# NEXT_PUBLIC_SENTRY_DSN=https://xxx@oXXX.ingest.sentry.io/XXX  # for client side

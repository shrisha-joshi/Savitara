---
# Savitara Kubernetes namespace
# Apply first: kubectl apply -f k8s/namespace.yaml
apiVersion: v1
kind: Namespace
metadata:
  name: savitara
  labels:
    name: savitara
    environment: production

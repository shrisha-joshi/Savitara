---
# ⚠️  TEMPLATE ONLY — Do NOT commit real secrets to git.
#
# Workflow:
#   1. Generate base64 values locally:
#         echo -n "my-secret-value" | base64
#   2. OR use Sealed Secrets (recommended):
#         kubeseal --format yaml < secrets-raw.yaml > secrets-sealed.yaml
#         kubectl apply -f secrets-sealed.yaml
#
# All secret values here are PLACEHOLDERS.
apiVersion: v1
kind: Secret
metadata:
  name: savitara-secrets
  namespace: savitara
  labels:
    app: savitara-backend
type: Opaque
data:
  # base64-encoded values — replace all CHANGEME entries before applying
  secret-key:           Q0hBTkdFTUU=   # echo -n "CHANGEME" | base64
  jwt-secret-key:       Q0hBTkdFTUU=
  mongodb-url:          Q0hBTkdFTUU=
  redis-url:            Q0hBTkdFTUU=
  google-client-id:     Q0hBTkdFTUU=
  google-client-secret: Q0hBTkdFTUU=
  razorpay-key-id:      Q0hBTkdFTUU=
  razorpay-key-secret:  Q0hBTkdFTUU=
  sentry-dsn:           Q0hBTkdFTUU=
  smtp-password:        Q0hBTkdFTUU=
  twilio-auth-token:    Q0hBTkdFTUU=

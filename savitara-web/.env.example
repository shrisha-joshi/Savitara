# ============================================================
# Savitara Web (Vite + React) — Environment Variables
# ============================================================
# 1. Copy:  cp .env.example .env
# 2. Vite exposes only VITE_ prefixed vars to the browser.
# 3. Never put secrets here — everything is visible in the bundle.
# ============================================================

# ── API ───────────────────────────────────────────────────────
# Dev:        http://localhost:8000/api/v1
# Production: https://api.savitara.com/api/v1
VITE_API_URL=http://localhost:8000
VITE_API_BASE_URL=http://localhost:8000/api/v1

# ── Google OAuth ──────────────────────────────────────────────
# Authorised JS origin: http://localhost:3000
# Authorised redirect URI: http://localhost:3000/auth/callback
VITE_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# ── Razorpay ──────────────────────────────────────────────────
# Use rzp_test_ key in dev, rzp_live_ key in production
VITE_RAZORPAY_KEY=rzp_test_your_key_here

# ── Firebase (Web Push Notifications) ────────────────────────
# Get these from Firebase Console → Project Settings → Your apps
VITE_FIREBASE_API_KEY=your-firebase-api-key
VITE_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your-project-id
VITE_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=123456789
VITE_FIREBASE_APP_ID=1:123456789:web:abcdef
VITE_FIREBASE_MEASUREMENT_ID=G-XXXXXXXXXX
VITE_FIREBASE_VAPID_KEY=your-vapid-key           # Cloud Messaging → Web Push certs

# ── WebSocket ─────────────────────────────────────────────────
VITE_SOCKET_URL=http://localhost:8000
VITE_BACKEND_WS_HOST=localhost:8000
VITE_BACKEND_HOST=localhost:8000

# ── App Meta ──────────────────────────────────────────────────
VITE_APP_NAME=Savitara
VITE_APP_VERSION=1.0.0
VITE_ENV=development                 # development | staging | production

# ── Sentry (optional) ────────────────────────────────────────
# VITE_SENTRY_DSN=https://xxx@oXXX.ingest.sentry.io/XXX
